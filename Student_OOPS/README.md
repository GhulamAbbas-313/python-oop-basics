# Student Class Project 🎓

This is a small Python project made to practice OOP (Object-Oriented Programming).

## 🔹 What this project does
- Creates a `Student` class with:
  - `name` and `marks` attributes  
  - `display_info()` method to show details  
  - `grade()` method to show grade based on marks  
- Uses constructor `__init__` and one class variable `school_name`.

## 🧠 Concepts Used
- Classes and Objects  
- Constructors  
- Instance & Class Variables  
- Methods  

## 🖥️ Example Output
